apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-backend-config
  namespace: demo
  labels:
    app.kubernetes.io/part-of: demo-3tier
data:
  DB_HOST: demo-postgres
  DB_PORT: "5432"
  PORT: "8080"
