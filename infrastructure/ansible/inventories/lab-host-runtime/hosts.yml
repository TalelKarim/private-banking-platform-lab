---
all:
  children:
    openstack_runtime_hosts:
      hosts:
        lab-host:
          ansible_host: "{{ lab_host_ansible_host }}"
          ansible_user: ubuntu
          ansible_python_interpreter: /usr/bin/python3
          ansible_ssh_private_key_file: "{{ lab_host_ssh_private_key_file }}"
