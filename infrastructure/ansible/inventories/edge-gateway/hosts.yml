---
all:
  children:
    edge_gateways:
      hosts:
        edge-gateway:
          ansible_host: "{{ edge_gateway_ansible_host }}"
          ansible_user: ubuntu
          ansible_python_interpreter: /usr/bin/python3
          ansible_ssh_private_key_file: "{{ edge_gateway_ssh_private_key_file }}"
