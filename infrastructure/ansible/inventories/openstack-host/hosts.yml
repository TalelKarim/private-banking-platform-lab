---
all:
  children:
    openstack_aio:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
          common_linux_hostname: lab-host
