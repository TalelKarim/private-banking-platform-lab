---
all:
  children:
    jenkins_controllers:
      hosts:
        jenkins-controller:
          ansible_host: "{{ jenkins_controller_ansible_host }}"
          ansible_user: ubuntu
          ansible_python_interpreter: /usr/bin/python3
          ansible_ssh_private_key_file: /home/ubuntu/.ssh/private-banking-openstack-workloads
